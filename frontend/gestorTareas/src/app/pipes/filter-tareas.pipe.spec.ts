import { FilterTareasPipe } from './filter-tareas.pipe';

describe('FilterTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
